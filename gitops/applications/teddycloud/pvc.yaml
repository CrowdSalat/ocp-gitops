apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: teddycloud
  namespace: app-teddycloud
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: lvms-vg1
  volumeMode: Filesystem
