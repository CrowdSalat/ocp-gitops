apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: owntone-cache
  namespace: app-teddycloud
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: lvms-vg1
  volumeMode: Filesystem
