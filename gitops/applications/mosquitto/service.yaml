apiVersion: v1
kind: Service
metadata:
  name: mosquitto
  namespace: app-teddycloud
  labels:
    app: mosquitto
spec:
  selector:
    app: mosquitto
  type: ClusterIP
  ports:
  - name: mqtt
    port: 1883
    targetPort: mqtt
    protocol: TCP
