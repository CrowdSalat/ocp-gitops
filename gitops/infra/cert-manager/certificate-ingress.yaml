apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ingress-wildcard
  namespace: openshift-ingress
spec:
  dnsNames:
  - "*.apps.ocp.jharings.de"
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  secretName: ingress-wildcard-tls
